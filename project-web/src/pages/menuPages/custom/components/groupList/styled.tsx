import styled from 'styled-components';

export const Wrapper = styled.div`
  display: flex;
  align-items: center;
  .add_icon {
    font-size: 32px;
    margin-left: 15px;
  }
  .add_icon_hide {
    opacity: 0;
    pointer-events: none;
  }
`;
