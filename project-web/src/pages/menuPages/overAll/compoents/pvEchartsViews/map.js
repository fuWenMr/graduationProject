// import DataSet from '@antv/data-set';
// import data from './mapdata.json';

function run() {
    // data set
    // const ds = new DataSet();

    // // draw the map
    // const dv = ds
    //     .createView('back')
    //     .source(data, {
    //         type: 'GeoJSON',
    //     })
    //     .transform({
    //         type: 'geo.projection',
    //         projection: 'geoMercator',
    //         as: ['x', 'y', 'centroidX', 'centroidY'],
    //     });
    // console.log(dv);
    // const bgView = new DataSet.View().source(dv.rows);
    // console.log('数据试图', bgView);
}

export default run;
